spring:
  application:
    name: service-applicationform
  cloud:
    config:
      label: dev
      profile: dev
      discovery:
        enabled: true
        service-id: service-config

server:
  port: 12451

eureka:
  client:
    service-url:
      defaultZone: http://localhost:12300/eureka/